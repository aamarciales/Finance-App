import { enUS } from 'date-fns/locale'

/** Default locale for dates shown in the UI. */
export const displayLocale = enUS

/** Default locale for numbers shown in the UI. */
export const numberLocale = 'en-US'
